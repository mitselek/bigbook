{
  "name": "bigbook-auth-proxy",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker proxying GitHub App OAuth token exchange + refresh for the bigbook bilingual reader.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
