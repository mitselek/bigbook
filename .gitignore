# Node / Astro
node_modules/
dist/
.astro/

# GH Actions assemble output (local dry-run artifact path)
_pages/

# Jekyll build output (legacy subdir, dry-run)
legacy/_site/
legacy/.jekyll-cache/

# Cloudflare Worker (sibling service in worker/)
worker/node_modules/
worker/.wrangler/
worker/.dev.vars

# Extracted-text working area
# extracted_text

# Superpowers brainstorming visual companion (mockup HTML, server state)
.superpowers/

# Playwright
playwright-report/
test-results/
playwright/.cache/

# Vitest coverage report
coverage/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Project-local tmp scratch (commit-msg drafts, etc.)
.tmp/

# Python virtualenv for PDF-extraction scripts (pymupdf, pdfplumber)
.venv/
