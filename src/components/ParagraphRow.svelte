<script lang="ts">
  interface Props {
    paraId: string
    enText: string
    etText: string
    isTitle: boolean
    isDiverged: boolean
    baselineEtText?: string
    chapterSlug: string
  }
  let { paraId, enText, etText, isTitle, isDiverged, baselineEtText, chapterSlug }: Props =
    $props()
</script>

<div>stub</div>
