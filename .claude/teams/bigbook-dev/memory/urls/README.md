Long URLs never sit inline in scratchpads or facts: one URL per file here, referenced
by the file name (ages-old shortener technique). Example: `worker-dashboard` contains the
Cloudflare dashboard URL; prose says "see urls/worker-dashboard".
