# Constants for the facts memory system. Spec: passepartout designs/facts-memory-redesign.md (spec of record)
# The facts state what's true; this file states the numbers.
FACTS_PER_FILE_MAX=30
FACT_WORDS_MAX=42
SLUG_WORDS_MAX=4
SPLIT_FLOOR_PCT=20
STUB_FACTS_MIN=6
SWEEP_NUM_OLDEST=10
STILL_TRUE_MAX_PER_WEEK=3
QUARTER_NUM_OF_DAYS=90
FACTS_DIR="${FACTS_DIR:-.claude/teams/bigbook-dev/memory/facts}"
