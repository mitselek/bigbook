/**
 * Produce a markdown report with 3 random blocks per section for PO proofreading.
 */

import type { BigBookEnglish } from './types'

export function buildSampleReview(_doc: BigBookEnglish, _seed = 42): string {
  throw new Error('buildSampleReview not implemented')
}
